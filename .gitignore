
# ── GSD baseline (auto-generated) ──
.gsd
.gsd-worktrees/
.gsd-backups/
.gsd-id
.mcp.json
.bg-shell/
.DS_Store
Thumbs.db
nul
nul.*
con
con.*
prn
prn.*
aux
aux.*
com[1-9]
com[1-9].*
lpt[1-9]
lpt[1-9].*
*.swp
*.swo
*~
.idea/
.vscode/*
!.vscode/extensions.json
*.code-workspace
.env
.env.*
!.env.example
node_modules/
.nuxt/
.next/
dist/
build/
__pycache__/
*.pyc
.venv/
venv/
target/
vendor/
*.log
coverage/
.cache/
tmp/
*.db
*.db-*
*.sql
*.dump
*.sqlite
*.sqlite3

# ── Recipe image uploads (T04) ──
# Files land in public/recipes/ at runtime via the image-upload flow. Keep the
# directory tracked (so the storage path always exists after clone) but never
# commit user-uploaded images.
public/recipes/*
!public/recipes/.gitkeep

# ── repowise (local index, caches, jobs) ──
.repowise/

# ── GSD baseline (auto-generated) ──
.vscode/
